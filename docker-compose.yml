services:
  chromadb:
    image: "chromadb/chroma:1.5.5"
    ports:
      - "8080:8000"
    volumes:
      - chroma-data:/data
      - ./chroma-config.yaml:/config.yaml

volumes:
  chroma-data:
    driver: local
